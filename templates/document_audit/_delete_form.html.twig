<form method="post" action="{{ path('app_document_audit_delete', {'id': document_audit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ document_audit.id) }}">
    <button class="btn">Delete</button>
</form>
