{% extends 'base.html.twig' %}

{% block title %}DocumentAudit{% endblock %}

{% block body %}
    <h1>DocumentAudit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_audit.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ document_audit.name }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ document_audit.path }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ document_audit.info }}</td>
            </tr>
            <tr>
                <th>Extension</th>
                <td>{{ document_audit.extension }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_document_audit_index') }}">back to list</a>

    <a href="{{ path('app_document_audit_edit', {'id': document_audit.id}) }}">edit</a>

    {{ include('document_audit/_delete_form.html.twig') }}
{% endblock %}
